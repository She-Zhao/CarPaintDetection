运行命令行：
    【目前使用】保存检测框图片，保存检测框标签txt文件：
        python detect.py --save-txt
    保存检测框图片，不保存检测框标签txt文件：
        python detect.py
    不保存检测框图片，保存检测框标签txt文件：
        python detect.py --no-save --save-txt
    不保存检测框图片，不保存检测框标签txt文件：
        python detect.py --no-save


更改绘制检测框的图片路径：
    修改‘detect.py’文件 155、156行：
        pos_num = path.split('\\')[-1].split('pos')[-1][0]  # 获取图片位置编号
        img2paint_path = f'/home/nvidia/demo/slave/output/Camera/pos' + str(pos_num) + '/gc0.png'   # 需要绘制检测框的图片路径


更改检测模型：
    修改‘detect.py’文件 329、330行：
        parser.add_argument('--weights', nargs='+', type=str, default=ROOT / 'weights/拟使用的模型文件名.pt',
                        help='model path or triton URL')